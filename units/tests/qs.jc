let filter(fn, array)
	| isEmpty(array) = []
	| fn(head(array)) = concat([head(array)], filter(fn, tail(array)))
	| else = filter(fn, tail(array))


let qs(array) 
	| isEmpty(array) = []
	| else = concat
		(
			concat(
				qs(
					filter(
						{(x) = x < head(array)}, 
						tail(array)
					)
				), 
				[head(array)]
			), 
			qs(
				filter(
					{(x) = x >= head(array)}, 
					tail(array)
				)
			)
		)
